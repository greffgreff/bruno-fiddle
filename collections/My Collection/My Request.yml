info:
  name: My Request
  type: http
  seq: 1

http:
  method: GET
  url: https://dummyjson.com/carts?v={{process.env.MY_KEY}}
  params:
    - name: v
      value: "{{process.env.MY_KEY}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
